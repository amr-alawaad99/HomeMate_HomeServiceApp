import 'package:flutter/material.dart';

class AppointmentTechnicalScreen extends StatelessWidget {
  const AppointmentTechnicalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
          'Appointment Technical'
      ),
    );
  }
}
